name: PartyRoom
main: partyroom.PartyRoom
author: Explodncheez
description: Runescape-inspired Party Room system.
version: 3.41
softdepend: [WorldGuard, WorldEdit, Vault]
api-version: 1.13

commands:
  proom:
    description: Base command for PartyRoom
    
permissions:
  partyroom:
    create:
      default: op
    commands:
      default: op
    withdraw:
      default: op
    help:
      default: true
    pull:
      default: true
    deposit:
      default: true
    view:
      default: true
    bypass:
      default: op